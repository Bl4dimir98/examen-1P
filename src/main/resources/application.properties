spring.application.name             =product.demo
spring.datasource.url               =jdbc:postgresql://localhost:5432/hbo-db
spring.datasource.username          =postgres
spring.datasource.password          =5366
# Configuración de Hibernate par actualizacion de la base de datos
spring.jpa.hibernate.ddl-auto       =update
spring.profiles.active              =bladimir


# Configurar puerto autmático para trabajar con Eureka
# server.port=0
# id de instancia única
# eureka.instance.instance-id=${spring.application.name}:${random.uuid}